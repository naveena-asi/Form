# Copy to .env and fill in to enable real AI form generation.
# Without this, AI Generate falls back to a local heuristic draft.
ANTHROPIC_API_KEY=sk-ant-...

# Optional — port for the AI backend (default 8787; Vite proxies /api here).
# PORT=8787
